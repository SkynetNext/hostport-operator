resources:
  - ../rbac
  - ../manager
  - ../webhook

namespace: operators

namePrefix: hostport-operator-

# [IMAGE] is set during 'make deploy'
images:
  - name: controller
    newName: controller
    newTag: latest
